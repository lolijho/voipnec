; =============================================================================
; MessageNet Trunk Configuration
; =============================================================================
; Auto-generated by AsteriskPanel. Do not edit manually.
; Configure via web UI: Trunk Manager > MessageNet
; =============================================================================

; Uncomment and configure after setting up via AsteriskPanel web UI

;[messagenet-registration]
;type=registration
;outbound_auth=messagenet-auth
;server_uri=sip:sip.messagenet.it
;client_uri=sip:USERNAME@sip.messagenet.it
;retry_interval=60
;fatal_retry_interval=30
;expiration=3600

;[messagenet-auth]
;type=auth
;auth_type=userpass
;username=USERNAME
;password=PASSWORD

;[messagenet-aor]
;type=aor
;contact=sip:sip.messagenet.it
;qualify_frequency=30

;[messagenet]
;type=endpoint
;transport=transport-udp
;context=from-trunk-messagenet
;disallow=all
;allow=alaw
;allow=ulaw
;allow=g729
;outbound_auth=messagenet-auth
;aors=messagenet-aor
;from_domain=sip.messagenet.it
;from_user=USERNAME
;direct_media=no
;rtp_symmetric=yes
;force_rport=yes
;rewrite_contact=yes

;[messagenet-identify]
;type=identify
;endpoint=messagenet
;match=sip.messagenet.it
